@model Cine_Nauta.Models.ShowCartViewModel

@{
    ViewData["Title"] = "Cart";
}

<link rel="stylesheet" href="https://cdn.datatables.net/1.13.3/css/jquery.dataTables.min.css" />

<div class="row">
    <div class="col-md-4">
        <dl class="row">
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Quantity)
            </dt>
            <dd class="col-sm-8">
                <h3>@Html.DisplayFor(model => model.Quantity)</h3>
            </dd>
            <dt class="col-sm-4">
                @Html.DisplayNameFor(model => model.Value)
            </dt>
            <dd class="col-sm-8">
                <h3>@Html.DisplayFor(model => model.Value)</h3>
            </dd>
        </dl>
    </div>
    <div class="col-md-8">
        <div class="col-md-12">
            <form asp-action="ShowCartAndConfirm">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>              
                <div class="form-group mt-2">
                    <input type="submit" value="Confirmar Pedido" class="btn btn-outline-primary" />
                    <a asp-action="Movies" class="btn btn-outline-success">Regresar</a>
                </div>
            </form>
        </div>
    </div>
</div>


<div class="row">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Carro de Compras</h3>
                <form asp-action="DeleteAll">
                    <input type="submit" style="margin-bottom: 10px" value="Borrar Todo" class="btn btn-outline-danger" />
                </form>
            </div>
            <div class="panel-body">
                <table class="table" id="MyTable">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.TemporalSales.FirstOrDefault().Function.Room.NumberRoom)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.TemporalSales.FirstOrDefault().Function.Movie.Title)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.TemporalSales.FirstOrDefault().Function.Price)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.TemporalSales.FirstOrDefault().Quantity)
                            </th>
                            <th></th>
                            <th>
                                @Html.DisplayNameFor(model => model.TemporalSales.FirstOrDefault().Value)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.TemporalSales)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Function.Room.NumberRoom)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Function.Movie.Title)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Function.Price)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Quantity)
                                </td>
                                <td class="d-flex flex-column">
                                    <a asp-action="IncreaseQuantity" asp-route-temporalSaleId="@item.Id" class="btn btn-primary mb-2">+</a>
                                    <br>
                                    @if (item.Quantity < 2)
                                    {
                                        <button asp-action="DecreaseQuantity" asp-route-temporalSaleId="@item.Id" class="btn btn-secondary" disabled>-</button>
                                    }
                                    else
                                    {
                                        <a asp-action="DecreaseQuantity" asp-route-temporalSaleId="@item.Id" class="btn btn-secondary">-</a>
                                    }
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Value)
                                </td>
                                <td>
                                    <a asp-action="EditTemporalSale" asp-route-temporalSaleId="@item.Id" class="btn btn-outline-warning">Editar</a>
                                    <a asp-action="DeleteTemporalSale" asp-route-temporalSaleId="@item.Id" class="btn btn-outline-danger">Borrar</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    <script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $('#MyTable').DataTable({
                "language": {
                    "url": "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json"
                },
                "aLengthMenu": [
                    [25, 50, 100, 200, -1],
                    [25, 50, 100, 200, "Todos"]
                ]
            });
        });
    </script>
}
